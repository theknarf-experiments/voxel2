[package]
name = "bevy_impostors"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Instanced point-scatter props for Bevy, and a crossed-silhouette impostor that draws a forest's worth of them in one call"

# In this workspace the bevy dependency inherits the root feature list.
# When extracted to its own repo this needs an explicit set: bevy_pbr,
# bevy_core_pipeline, bevy_render and bevy_asset's embedded assets.
[dependencies]
bevy.workspace = true
bevy_pbr_view.workspace = true
bytemuck.workspace = true
